import { Service } from "encore.dev/service";

// Encore.ts Backend API
// Encore will consider this directory and all its subdirectories as part of the anotherService service.
export default new Service("api");
