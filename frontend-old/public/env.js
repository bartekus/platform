window.__ENV__ = {
    // In production, these values will be injected at the run time using nginx and frontend/public/env.js.tmpl
    // VITE_LOGTO_APP_ID:   "${LOGTO_APP_ID}",
    // VITE_LOGTO_DOMAIN:   "${LOGTO_DOMAIN}",
    // VITE_API_DOMAIN:     "${API_DOMAIN}",
    // VITE_WEB_DOMAIN:     "${WEB_DOMAIN}"
    // In development we leverage Vite dev server to inject these values using import.meta.env
};